<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:id="@+id/diet_group"
        android:visible="false">

        <item
            android:id="@+id/diet_action_menu"
            android:orderInCategory="10"
            android:icon="@drawable/ic_diet2"
            android:title="@string/add_food_button_title"
            app:showAsAction="ifRoom" />

    </group>

    <item
        android:id="@+id/add_food_action_menu"
        android:orderInCategory="20"
        android:icon="@drawable/ic_add_food_button"
        android:title="@string/diet_button_title"
        app:showAsAction="ifRoom" />
</menu>